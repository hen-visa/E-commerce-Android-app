<?xml version="1.0" encoding="utf-8"?>
<!--
  AndroidManifest.xml
  REPLACE the existing AndroidManifest.xml with this file.
  Changes:
    + android.permission.INTERNET
    + android.permission.ACCESS_NETWORK_STATE
    - Removed stale ProfileActivity (it is a Fragment now)
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required for Retrofit / OkHttp (REST API calls) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".ECommerceApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ECommerce"
        android:usesCleartextTraffic="false"
        tools:targetApi="31">

        <activity
            android:name=".activities.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.DashboardActivity"
            android:exported="true" />

        <activity
            android:name=".activities.CartActivity"
            android:exported="false" />

        <activity
            android:name=".activities.DetailActivity"
            android:exported="false" />

        <activity
            android:name=".activities.OrdersActivity"
            android:exported="false" />

        <activity
            android:name=".activities.AuthActivity"
            android:exported="false" />

        <activity
            android:name=".activities.CategoryActivity"
            android:exported="false" />

        <activity
            android:name=".activities.AddressActivity"
            android:exported="false" />

        <activity
            android:name=".activities.ConfirmOrderActivity"
            android:exported="false" />

    </application>

</manifest>
